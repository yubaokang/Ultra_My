<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.srain.sube.views.ptr.demo">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="in.srain.cube.views.ptr.demo.app.PtrDemoApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name="in.srain.cube.views.ptr.demo.ui.activity.PtrDemoHomeActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name="in.srain.cube.views.ptr.demo.ui.viewpager.ViewPagerActivity" />
    </application>

</manifest>
